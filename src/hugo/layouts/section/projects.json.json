{{- $index := slice -}}
{{- range .Pages -}}
  {{- $index = $index | append (dict
    "title" .Title
    "content" .Plain
    "categories" .Params.categories
    "card" (partial "vertical-card.html" . | base64Encode))
  -}}
{{- end -}}

{{- $index | jsonify -}}
